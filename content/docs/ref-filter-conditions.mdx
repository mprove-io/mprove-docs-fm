---
title: Filter Conditions
---

import { DynamicCodeBlock } from "@/components/dynamic-codeblock";
import { filterConditionsStringYaml } from "@/content/docs/parts/filter-conditions-string-yaml";
import { filterConditionsNumberYaml } from "@/content/docs/parts/filter-conditions-number-yaml";
import { filterConditionsBooleanYaml } from "@/content/docs/parts/filter-conditions-boolean-yaml";
import { filterConditionsTimestampYaml } from "@/content/docs/parts/filter-conditions-timestamp-yaml";

# Filter Conditions

<Callout>
  Reports, Charts, Dashboards are intended to be created and modified through the
  user interface. The file representation is for checking errors during
  validation.
</Callout>

Filter conditions is an array of [Malloy Filter Expressions](https://docs.malloydata.dev/documentation/language/filter-expressions). They can be applied to the fields of Malloy models.

In Mprove:

- All non-negated expressions in the array are combined with "OR"
- All negated expressions in the array are combined with "AND"
- Negated and non-negated expressions cannot be mixed in a single element of the conditions array (they must be split into separate elements of the conditions array)

## Examples

### String

Multiple expressions of string filter can only be separated by the "," character, otherwise they must be specified as separate array elements.

<DynamicCodeBlock
  lang="yaml"
  code={filterConditionsStringYaml}
  options={{
    themes: {
      light: "catppuccin-latte",
      dark: "rose-pine",
    },
  }}
/>

### Number

Multiple non-negated expressions of number filter can only be separated by the "or", otherwise they must be specified as separate array elements.

Multiple negated expressions of number filter can only be separated by the "and", otherwise they must be specified as separate array elements.

<DynamicCodeBlock
  lang="yaml"
  code={filterConditionsNumberYaml}
  options={{
    themes: {
      light: "catppuccin-latte",
      dark: "rose-pine",
    },
  }}
/>

### Boolean

<DynamicCodeBlock
  lang="yaml"
  code={filterConditionsBooleanYaml}
  options={{
    themes: {
      light: "catppuccin-latte",
      dark: "rose-pine",
    },
  }}
/>

### Timestamp

Multiple non-negated expressions of time filter can only be separated by the "or", otherwise they must be specified as separate array elements.

Multiple negated expressions of time filter can only be separated by the "and", otherwise they must be specified as separate array elements.

<DynamicCodeBlock
  lang="yaml"
  code={filterConditionsTimestampYaml}
  options={{
    themes: {
      light: "catppuccin-latte",
      dark: "rose-pine",
    },
  }}
/>
